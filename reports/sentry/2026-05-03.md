# Sentry Daily Triage — 2026-05-03 UTC

## Summary
- Total issues reviewed: 3
- Auto-fix PRs opened: 0
- Issues requiring human review: 3

## Auto-fix PRs
_None._

## Requires human review

---

### Error: Call to function 'ExpoUpdates.checkForUpdateAsync' has been rejected

- **Sentry**: https://sy-labs.sentry.io/issues/REACT-NATIVE-1
- **Frequency**: 1 event / 1 user in 24h
- **Stack (top frame)**: `app/_layout.tsx:107` — `Updates.checkForUpdateAsync()`
- **Device**: Samsung SM-S921N (Galaxy S24), Android 16, Korea (Mokpo-si)
- **Device online at time of error**: `false`
- **Release**: `com.sylabs.amatda@2.8.1+3` (OTA channel: `preview`, runtime: `2.8.1`)
- **Handled**: yes (caught in try/catch; no crash)

**Root cause hypothesis**:
`checkForUpdateAsync()` is called unconditionally 2 seconds after app start (`useOTAUpdate` hook in `app/_layout.tsx:154`). When the device has no network connection (`online: false` confirmed in event context), the native `ExpoUpdates` module rejects the promise with `Failed to check for update`. The catch block at line 139 calls `captureError()` unconditionally, sending the error to Sentry even though this is an expected offline condition.

**Why not auto-fixed**:
The fix requires a meaningful logic change to the async startup flow — either:
1. Add a `NetInfo.fetch()` pre-condition guard before calling `checkForUpdateAsync()` (introduces a new async call in the startup chain), or
2. Inspect the caught error in the catch block and skip `captureError` for network errors (requires defining what qualifies as a network error).
Neither maps cleanly to the auto-fix categories (typo / null guard / missing import / type annotation), and both touch app bootstrap logic where a mistake could silently break OTA delivery.

**Suggested action**:
Add an early-exit guard in `checkAndApply()` (`app/_layout.tsx:101`) using the already-installed `@react-native-community/netinfo` (v11.4.1):

```typescript
import NetInfo from '@react-native-community/netinfo';

const checkAndApply = useCallback(async () => {
  if (__DEV__ || checkingRef.current) return;
  const net = await NetInfo.fetch();          // skip silently when offline
  if (!net.isConnected) return;
  checkingRef.current = true;
  // … rest unchanged
}, []);
```

This prevents the error from being thrown in the first place and keeps Sentry clean for genuine OTA failures. No new dependency needed.

---

### Error: TOKEN_ENCRYPTION_KEY 형식 오류: hex 64자(32바이트) 필요. 현재 길이: 65

- **Sentry**: https://sy-labs.sentry.io/issues/REACT-NATIVE-2
- **Frequency**: 3 events / 0 users in 24h
- **Stack (top frame)**: `dist/src/utils/crypto.js:44` — `loadKey()`
- **Culprit**: `POST /api/auth/social`
- **Handled**: yes (caught at route level; returns HTTP 500)

**Root cause hypothesis**:
The `TOKEN_ENCRYPTION_KEY` environment variable set in the Firebase Cloud Functions deployment contains **65 hex characters** instead of the required 64 (32 bytes). The validation guard in `crypto.js:43` — `if (hex.length !== 64)` — throws, causing every social login attempt that reaches `encryptToken()` to fail with a 500. This is a pure configuration error; the validation code itself is correct.

Note: the source file `backend/src/utils/crypto.ts` does not exist in the current local checkout — it may have been deleted from the repo while the compiled `dist/` artifact remains deployed. This warrants a separate check.

**Why not auto-fixed**:
Configuration issue (wrong environment variable value in the Cloud Functions deployment), not a source-code bug. Fixing requires updating the `TOKEN_ENCRYPTION_KEY` secret in Firebase project settings or the `.env` / `runtimeconfig` used during deployment. Additionally, the missing source file (`crypto.ts`) should be investigated.

**Suggested action**:
1. Verify the current value: `firebase functions:config:get` or check the Secret Manager entry for `TOKEN_ENCRYPTION_KEY`. Count the characters — there is likely a trailing character (newline, extra digit, or copy-paste artifact) that makes it 65 instead of 64.
2. Re-set with exactly 64 hex chars: `openssl rand -hex 32` produces the correct format.
3. Redeploy the function after fixing the secret.
4. Confirm `backend/src/utils/crypto.ts` still exists in the repo or restore it — a compiled artifact without a source file is a maintainability risk.

---

### Error: 9 FAILED_PRECONDITION: The query requires an index

- **Sentry**: https://sy-labs.sentry.io/issues/REACT-NATIVE-3
- **Frequency**: 9 events / 0 users in 24h
- **Stack (top frame)**: `node_modules/@google-cloud/firestore/build/src/reference/query.js:745`
- **Culprit**: `GET /api/mom-group/posts/radius`
- **Handled**: yes (caught at route level; falls back to context `"mom-group:fallback"`)

**Root cause hypothesis**:
The `GET /api/mom-group/posts/radius` endpoint queries the `momGroupPosts` Firestore collection with a compound filter that includes `hidden` (equality or range) + `createdAt` (order/range) + potentially a geo-hash or `__name__` sort. Firestore requires a composite index for any query that combines a filter on one field with ordering on a different field. The index does not exist, so all requests to this endpoint fail with `FAILED_PRECONDITION`.

Firebase provided the direct index-creation URL in the error message:
`https://console.firebase.google.com/v1/r/project/amatda-parenting/firestore/indexes?create_composite=ClZwcm9qZWN0cy9hbWF0ZGEtcGFyZW50aW5nL2RhdGFiYXNlcy8oZGVmYXVsdCkvY29sbGVjdGlv...`

The decoded index is on collection `momGroupPosts`: `hidden ASC, createdAt DESC, __name__ DESC`.

Note: `mom-group` route is not present in `backend/src/routes/` in the current local checkout — the endpoint may be deployed but the source is not checked in (similar situation to `crypto.ts`).

**Why not auto-fixed**:
Infrastructure fix — requires creating a Firestore composite index via the Firebase console, not a code change. The CLAUDE.md rules also prohibit modifying Firestore security rules (and by extension infrastructure config) without human review.

**Suggested action**:
1. Open the Firebase-provided URL directly to create the index with one click (it pre-fills all fields).
2. Alternatively, add the index to `firestore.indexes.json` and run `firebase deploy --only firestore:indexes` to manage it as code.
3. Investigate why the `mom-group` route source is missing from the local checkout — locate it, add it to the repo, and ensure `firestore.indexes.json` is kept in sync.
