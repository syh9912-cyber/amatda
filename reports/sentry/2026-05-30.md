# 앱 오류 일일 리포트 — 2026년 05월 30일

## 한눈에 보기

- 전체 미해결 이슈: **18건**
- 최근 24시간 내 재발생: **2건**
- 자동으로 고친 것: **0건**
- 사장님이 판단할 것: **7건** (아래 상세 참조)
- 이미 코드에서 없어진 낡은 이슈 (Sentry 닫기 권고): **5건**

> **오늘의 핵심**: 자동 업데이트(OTA) 확인 오류가 가장 많이 발생 중이지만 앱이 죽지는 않습니다. 보이스 화면에 구조적인 문제가 있어 수정이 필요합니다. Firebase 인덱스 누락 문제는 '내 근처 엄마 모임' 기능을 완전히 막고 있습니다.

---

## ❓ 사장님이 판단할 것

### 1. 앱 자동 업데이트 확인이 가끔 실패함 (최근 24시간 재발생)
- **얼마나 자주 발생하나**: 지난 24시간 동안 6번 발생, 사용자 4명에게 발생 (전체 기간 누적 26번, 사용자 9명)
- **어떤 상황에서 발생하나**: 앱이 켜질 때 또는 화면이 꺼졌다가 다시 켜질 때, 새 버전이 있는지 서버에 확인하는 과정에서 네트워크 오류로 실패
- **사용자가 실제로 겪는 것**:
  - 🟢 **가벼움** — 사용자는 모름. 앱이 자동으로 오류를 처리하고 정상 실행함. Sentry 로그만 쌓임
- **추천**: ✅ **그냥 둬도 됨**
- **추천 이유**: 앱 코드가 이미 이 오류를 잡아서 처리하고 있음 (`handled: yes`). 사용자는 아무 불편을 느끼지 않음. 단, Sentry에 불필요한 로그가 쌓이는 것이 거슬리면 "Sentry에 이 오류를 보고하지 않도록" 수정 가능.
- **고친다면 이렇게**:
  - 수정할 파일: `frontend/app/_layout.tsx`
  - 수정 방향: OTA 업데이트 확인 실패 시 Sentry에 보고하지 않도록 catch 블록 수정 (1줄 변경)
  - 사장님이 Claude 에게 시킬 말 예시: `"앱 자동 업데이트 확인 실패 오류를 Sentry에 보고하지 않도록 고쳐줘"`

<details>
<summary>개발자용 세부 정보 (사장님은 안 읽어도 됨)</summary>

- Sentry 링크: [REACT-NATIVE-F](https://sy-labs.sentry.io/issues/REACT-NATIVE-F), [REACT-NATIVE-1](https://sy-labs.sentry.io/issues/REACT-NATIVE-1), [REACT-NATIVE-B](https://sy-labs.sentry.io/issues/REACT-NATIVE-B), [REACT-NATIVE-8](https://sy-labs.sentry.io/issues/REACT-NATIVE-8), [REACT-NATIVE-4](https://sy-labs.sentry.io/issues/REACT-NATIVE-4)
- 스택 최상단: `_layout.tsx` → `Updates.checkForUpdateAsync()` → "Failed to check for update"
- 환경: Android (production), online: true
- 처리됨 여부: handled (catch 블록에서 captureError 호출 후 status='idle' 복귀)
- `_layout.tsx:139-144`: catch 블록에서 `captureError(error, { context: 'OTA update check' })` 호출 — 의도적으로 Sentry에 보고하는 코드가 있음

</details>

---

### 2. 보이스 화면에서 앱이 죽는 오류 (4일 전 발생)
- **얼마나 자주 발생하나**: 지난 4일간 1번, 사용자 1명에게 발생
- **어떤 상황에서 발생하나**: 사용자가 보이스(음성 입력) 화면을 열었을 때, 앱이 완전히 준비되기 전에 화면 이동 명령이 실행됨
- **사용자가 실제로 겪는 것**:
  - 🔴 **심각** — 앱이 죽음 (`handled: no`, `level: fatal`)
- **추천**: 🔧 **고치는 게 좋음**
- **추천 이유**: 앱이 실제로 죽는(강제 종료되는) 오류임. 발생 빈도는 낮지만 음성 기능을 사용하는 사용자가 앱이 죽는 경험을 하면 신뢰도에 악영향
- **고친다면 이렇게**:
  - 수정할 파일: `frontend/app/voice.tsx`
  - 수정 방향: `useEffect` 내에서 `router.replace` 호출 시 Root Layout이 준비됐는지 확인 후 실행하도록 수정 (Expo Router 공식 패턴 적용)
  - 사장님이 Claude 에게 시킬 말 예시: `"보이스 화면 열 때 앱이 죽는 오류 고쳐줘 (REACT-NATIVE-J)"`

<details>
<summary>개발자용 세부 정보 (사장님은 안 읽어도 됨)</summary>

- Sentry 링크: [REACT-NATIVE-J](https://sy-labs.sentry.io/issues/REACT-NATIVE-J), 유사 이슈: [REACT-NATIVE-G](https://sy-labs.sentry.io/issues/REACT-NATIVE-G), [REACT-NATIVE-A](https://sy-labs.sentry.io/issues/REACT-NATIVE-A)
- 스택 최상단: `app/voice.tsx:useEffect` → `router.replace` → `assertIsReady()` throw
- 환경: Android 16 / SM-S921N / production
- 처리됨 여부: unhandled (fatal crash)
- 원인: Expo Router의 Root Layout이 마운트되기 전에 `router.replace`가 실행됨. `useEffect`에서 인증 상태 확인 즉시 이동 시도.

</details>

---

### 3. 보이스 화면에서 React Hook 규칙 위반 오류 (23일 전 발생)
- **얼마나 자주 발생하나**: 23일간 2번, 사용자 1명에게 발생
- **어떤 상황에서 발생하나**: 사용자가 보이스(음성 입력) 화면을 열고 음성 인식이 시작될 때
- **사용자가 실제로 겪는 것**:
  - 🟡 **보통** — 음성 인식 기능이 동작하지 않음 (앱 죽지는 않음)
- **추천**: 🔧 **고치는 게 좋음**
- **추천 이유**: React Hook을 잘못된 위치에서 사용 중 (2번 이슈와 함께 수정 권장). 음성 입력 기능의 신뢰성 문제
- **고친다면 이렇게**:
  - 수정할 파일: `frontend/app/voice.tsx`
  - 수정 방향: `useSpeechRecognitionEvent` (React Hook)를 `useCallback` 콜백 내부에서 호출하는 구조를 컴포넌트 최상위 레벨로 이동
  - 사장님이 Claude 에게 시킬 말 예시: `"보이스 화면의 음성 인식 Hook 오류 고쳐줘 (REACT-NATIVE-9)"`

<details>
<summary>개발자용 세부 정보 (사장님은 안 읽어도 됨)</summary>

- Sentry 링크: [REACT-NATIVE-9](https://sy-labs.sentry.io/issues/REACT-NATIVE-9)
- 스택 최상단: `app/voice.tsx:setupSpeechEvents` → `mod.useSpeechRecognitionEvent` → `useRef` → throwInvalidHookError
- 환경: Android 16 / SM-S921N / v2.8.1+3
- 처리됨 여부: handled (onunhandledrejection)
- 원인: `useSpeechRecognitionEvent`는 React Hook인데, `useCallback` 콜백 함수 내부(`setupSpeechEvents`)에서 호출 중. 이 콜백은 `initSpeechRecognition` 비동기 함수에서 호출되므로 React 렌더링 컨텍스트를 벗어남.

</details>

---

### 4. '내 근처 엄마 모임' 기능 전혀 안 됨 (27일 전부터 지속 발생)
- **얼마나 자주 발생하나**: 지난 27일간 21번 발생 (지속 발생 중)
- **어떤 상황에서 발생하나**: 사용자가 '내 근처 엄마 모임' 기능을 사용하려 할 때마다 항상 실패
- **사용자가 실제로 겪는 것**:
  - 🔴 **심각** — 기능이 완전히 안 됨. 근처 게시글을 불러오지 못함
- **추천**: 🔧 **고치는 게 좋음**
- **추천 이유**: Firebase 데이터베이스에 필요한 '색인(index)' 이 없어서 조회가 아예 불가능한 상태. 발생 횟수가 21번으로 많고 계속 실패 중. Firebase 콘솔에서 클릭 몇 번으로 해결 가능
- **고친다면 이렇게**:
  - 수정할 파일: Firebase Console (코드 수정 불필요)
  - 수정 방향: 오류 메시지 안에 포함된 링크를 클릭하면 Firebase가 자동으로 필요한 인덱스를 생성해 줌
  - Firebase 콘솔 인덱스 생성 링크: `https://console.firebase.google.com/v1/r/project/amatda-parenting/firestore/indexes?create_composite=ClZwcm9qZWN0cy9hbWF0ZGEtcGFyZW50aW5nL2RhdGFiYXNlcy8oZGVmYXVsdCkvY29sbGVjdGlv...`
  - 사장님이 Claude 에게 시킬 말 예시: `"내 근처 엄마 모임 조회 오류 고쳐줘 (REACT-NATIVE-3), Firebase 인덱스도 같이 처리해줘"`

<details>
<summary>개발자용 세부 정보 (사장님은 안 읽어도 됨)</summary>

- Sentry 링크: [REACT-NATIVE-3](https://sy-labs.sentry.io/issues/REACT-NATIVE-3)
- 스택 최상단: `GET /api/mom-group/posts/radius` → Firestore Query → `9 FAILED_PRECONDITION`
- 환경: node / production
- 처리됨 여부: handled
- 필요한 인덱스: `momGroupPosts` 컬렉션, `hidden ASC` + `createdAt DESC` + `__name__ DESC` 복합 인덱스

</details>

---

### 5. 구글 로그인 시 토큰 검증 오류 (최근 24시간 재발생)
- **얼마나 자주 발생하나**: 지난 24시간 동안 12번 발생 (로그인 시도 횟수 기준), 사용자 0명 영향
- **어떤 상황에서 발생하나**: 사용자가 구글 계정으로 로그인 시도할 때
- **사용자가 실제로 겪는 것**:
  - 🟢 **가벼움** — 실제 코드를 확인한 결과, 이 오류를 발생시킨 예전 코드가 이미 수정됨. 현재 앱 코드는 다른 방식(Google userinfo 엔드포인트)을 사용하고 있음. **Sentry에 오래된 기록이 남아있는 것임**
- **추천**: ✅ **그냥 둬도 됨** (Sentry에서 '해결됨'으로 닫기만 하면 됨)
- **추천 이유**: 코드가 이미 수정되어 현재 버전에는 이 문제가 없음. 이전 버전에서 발생한 이력이 Sentry에 남아 있는 것

<details>
<summary>개발자용 세부 정보 (사장님은 안 읽어도 됨)</summary>

- Sentry 링크: [REACT-NATIVE-6](https://sy-labs.sentry.io/issues/REACT-NATIVE-6), [REACT-NATIVE-7](https://sy-labs.sentry.io/issues/REACT-NATIVE-7)
- 현재 `social.auth.ts`의 `verifyGoogleToken`은 `/tokeninfo` 대신 `/oauth2/v3/userinfo` 사용. audience 검증 로직 제거됨.
- 처리됨 여부: handled

</details>

---

### 6. 결제(인앱결제) 처리 중 데이터 오류
- **얼마나 자주 발생하나**: 5일 전 8번 발생, 이후 발생 없음
- **어떤 상황에서 발생하나**: 인앱결제 처리 중
- **사용자가 실제로 겪는 것**:
  - 🟢 **가벼움** — 실제 코드를 확인한 결과, 해당 결제 처리 경로(`/api/payment/iap/verify`)가 현재 코드에서 제거됨. **Sentry에 오래된 기록이 남아있는 것임**
- **추천**: ✅ **그냥 둬도 됨** (Sentry에서 '해결됨'으로 닫기만 하면 됨)

<details>
<summary>개발자용 세부 정보 (사장님은 안 읽어도 됨)</summary>

- Sentry 링크: [REACT-NATIVE-H](https://sy-labs.sentry.io/issues/REACT-NATIVE-H)
- `POST /api/payment/iap/verify` 라우트가 현재 backend 코드에 존재하지 않음. 코드 정리 시 제거된 것으로 판단.
- 오류 내용: Firestore에 `raw.state: undefined` 저장 시도 → Firestore가 undefined를 거부

</details>

---

### 7. 인증 토큰 생성 오류 및 암호화 키 오류 (26~27일 전 발생)
- **얼마나 자주 발생하나**: 26~27일 전 각 3~4번 발생, 이후 발생 없음
- **사용자가 실제로 겪는 것**:
  - 🟢 **가벼움** — 실제 코드를 확인한 결과, 해당 코드가 이미 단순화/제거됨. **Sentry에 오래된 기록이 남아있는 것임**
- **추천**: ✅ **그냥 둬도 됨** (Sentry에서 '해결됨'으로 닫기만 하면 됨)

<details>
<summary>개발자용 세부 정보 (사장님은 안 읽어도 됨)</summary>

- Sentry 링크: [REACT-NATIVE-5](https://sy-labs.sentry.io/issues/REACT-NATIVE-5), [REACT-NATIVE-2](https://sy-labs.sentry.io/issues/REACT-NATIVE-2)
- REACT-NATIVE-5: `generateTokens`에서 `jti`를 payload와 options 양쪽에 중복 전달 → 현재 코드는 단순 `jwt.sign({userId}, secret, {expiresIn})` 구조로 변경됨
- REACT-NATIVE-2: `crypto.ts` 유틸에서 TOKEN_ENCRYPTION_KEY 길이 65자 → 현재 `utils/` 폴더에 `crypto.ts` 없음 (파일 제거됨)

</details>

---

## ℹ️ 참고: 시스템이 정상 작동 중인 항목

아래 2건은 오류처럼 보이지만 실제로는 시스템이 의도대로 작동 중인 것입니다.

| 이슈 | 내용 | 상태 |
|------|------|------|
| [REACT-NATIVE-D](https://sy-labs.sentry.io/issues/REACT-NATIVE-D) | AI 응답에 금지 용어 포함 → 자동 교체 | ✅ 시스템 정상 작동 (안전 필터가 막아서 다른 답변으로 교체함) |
| [REACT-NATIVE-C](https://sy-labs.sentry.io/issues/REACT-NATIVE-C) | Gemini AI 서버 과부하 | ✅ 외부 서비스 일시적 문제, 앱 코드 이슈 아님 |

---

## 📋 전체 이슈 현황 요약

| 이슈 ID | 내용 | 최근 발생 | 심각도 | 추천 |
|---------|------|-----------|--------|------|
| REACT-NATIVE-F/1/B/8/4 | 자동 업데이트 확인 실패 | 1일~26일 전 | 🟢 가벼움 | ✅ 그냥 둬도 됨 |
| REACT-NATIVE-J/G/A | 보이스 화면 앱 죽음 | 4일~23일 전 | 🔴 심각 | 🔧 고치기 |
| REACT-NATIVE-9 | 보이스 화면 음성인식 오류 | 23일 전 | 🟡 보통 | 🔧 고치기 |
| REACT-NATIVE-3 | 엄마 모임 기능 안 됨 | 27일 전~현재 | 🔴 심각 | 🔧 고치기 |
| REACT-NATIVE-6/7 | 구글 로그인 오류 (구 코드) | 1일~26일 전 | 🟢 해결됨 | Sentry 닫기 |
| REACT-NATIVE-H | 결제 처리 오류 (구 코드) | 5일 전 | 🟢 해결됨 | Sentry 닫기 |
| REACT-NATIVE-5/2 | 토큰 오류 (구 코드) | 26~27일 전 | 🟢 해결됨 | Sentry 닫기 |
| REACT-NATIVE-E | SVG 라이브러리 오류 (구 버전) | 22일 전 | 🟢 구 버전 | Sentry 닫기 |
| REACT-NATIVE-D | AI 금지어 필터 작동 | 8일 전 | ✅ 정상 | 유지 |
| REACT-NATIVE-C | Gemini AI 서버 과부하 | 19일 전 | ✅ 외부 | 유지 |
