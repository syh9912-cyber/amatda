# Sentry Daily Triage — 2026-05-04 UTC

## Summary
- Total issues reviewed: 4
- Auto-fix PRs opened: 0
- Issues requiring human review: 4

## Auto-fix PRs
_None — all issues require human action._

## Requires human review

---

### REACT-NATIVE-1 & REACT-NATIVE-4: ExpoUpdates.checkForUpdateAsync rejected (offline device)

- **Sentry REACT-NATIVE-1**: https://sy-labs.sentry.io/issues/REACT-NATIVE-1
- **Sentry REACT-NATIVE-4**: https://sy-labs.sentry.io/issues/REACT-NATIVE-4
- **Frequency**: 1 event / 1 user each (2 total, different Samsung Galaxy S21/S24 devices, Mokpo KR)
- **Stack (top frame)**: `expo-modules-core/src/errors/CodedError.ts:11` → `ExpoUpdates.checkForUpdateAsync`
- **Root cause hypothesis**: Both devices had `online: false` in device context at time of error. The OTA update check (`checkForUpdateAsync`) is called at app startup but the device had no network connection, causing the native module to reject the promise with "Failed to check for update". The error is already `handled: yes` so it does not crash the app. REACT-NATIVE-4's culprit misleadingly shows `constants/dailyQuestions.ts:130` because of minified stack unwinding, not because that file is the source of the error.
- **Why not auto-fixed**: Not a code defect — this is expected behavior when a device is offline. No source change is needed.
- **Suggested action**: If these become frequent (many users), consider adding an explicit network connectivity check (e.g., `NetInfo.fetch()`) before calling `checkForUpdateAsync`, and silently skip the OTA check when offline. For now (2 isolated events, 0 crashes), monitoring is sufficient.

---

### REACT-NATIVE-2: TOKEN_ENCRYPTION_KEY 형식 오류 (65 chars instead of 64)

- **Sentry**: https://sy-labs.sentry.io/issues/REACT-NATIVE-2
- **Frequency**: 3 events / 0 users in 24h (backend server-side error on `POST /api/auth/social`)
- **Stack (top frame)**: `dist/src/utils/crypto.js:44` (`loadKey`) ← called from `dist/src/routes/auth.js:129`
- **Root cause hypothesis**: The `TOKEN_ENCRYPTION_KEY` Firebase/Cloud Run environment variable contains **65 hexadecimal characters** instead of the required **64** (= 32 bytes). The most likely cause is a trailing newline (`\n`) or an accidental extra character when the secret was set. Note: `crypto.ts` no longer exists in the current source tree, but the compiled `dist/` deployed to Cloud Functions still contains this older code path (`encryptToken(socialUser.accessToken)`) — meaning the deployed function is running stale code relative to the current source.
- **Why not auto-fixed**: This is a Firebase environment variable configuration issue, not a code bug. It requires correcting the secret in the Firebase Console or via `firebase functions:secrets:set TOKEN_ENCRYPTION_KEY`. Additionally, the deployed dist is out of sync with the current source, which requires a redeployment.
- **Suggested action**:
  1. Fix env var: `printf '%s' '<64-char-hex>' | firebase functions:secrets:set TOKEN_ENCRYPTION_KEY` (use `printf` not `echo` to avoid trailing newline).
  2. Redeploy backend: `cd backend && npm run build && firebase deploy --only functions` to sync deployed dist with current source (which no longer has the `encryptToken` call in auth.ts).

---

### REACT-NATIVE-3: Firestore missing composite index — mom-group/posts/radius

- **Sentry**: https://sy-labs.sentry.io/issues/REACT-NATIVE-3
- **Frequency**: 9 events / 0 users in 24h (backend, `GET /api/mom-group/posts/radius`)
- **Stack (top frame)**: Firestore SDK `query.js:745` — gRPC FAILED_PRECONDITION
- **Root cause hypothesis**: The Firestore query on the `momGroupPosts` collection (filters: `hidden == false`, order by `createdAt DESC`) requires a composite index that has not been created. The error message contains a direct Firebase Console link to create the index. Additionally, the route `GET /api/mom-group/posts/radius` does not exist in the current source (`index.ts` has no `mom-group` route registration), confirming the deployed Cloud Function dist is running stale/older code.
- **Why not auto-fixed**: Requires creating a Firestore composite index via Firebase Console (infrastructure action), and a backend redeployment to sync source with dist.
- **Suggested action**:
  1. Create the missing index using the direct link from the error: `https://console.firebase.google.com/v1/r/project/amatda-parenting/firestore/indexes?create_composite=ClZwcm9qZWN0cy9hbWF0ZGEtcGFyZW50aW5nL2RhdGFiYXNlcy8oZGVmYXVsdCkvY29sbGVjdGlv...`
  2. Redeploy backend to sync deployed dist with current source.
  3. Confirm whether the `mom-group` feature is intentionally removed from source or still needed — if removed, the old deployed route is serving dead traffic.
