# 아맞다 앱 화면 감사 — 수정 전 검토 보고서

## 요약

- **총 결함 수: 159개** (56개 화면 중 47개 파일에서 발견)
- **High: 36개 / Medium: 58개 / Low: 65개**
- **공통 반복 패턴: 5개** (Android includeFontPadding 미설정, 빈 catch 무음 처리, 무한 스피너, 가짜 로딩, 미사용 데드코드)
- **가장 위험한 3가지:**
  1. **사주/오행 원천값 UI 직접 노출** (result.tsx, momstagram-post.tsx, analysis-report.tsx) — 핵심 제품 원칙 1 위반, 출시 차단급
  2. **무한 스피너 다발** (intake-form, monthly-characteristic, diary, recommendation-detail 등 — selectedChild 없을 때 loading이 영원히 true)
  3. **가짜 로딩 / 가짜 데이터** (questions 15초 강제, ai-analysis 2초, growth-stats 시뮬레이션 진행바, nutrition 가짜 추천도%) — Mock 금지 규칙 위반

---

## 공통 패턴 (여러 화면 반복)

### [공통][스타일] Android `includeFontPadding` 미설정 — 글리프 수직 정렬 어긋남
원형 배지/버튼/이모지/작은 폰트에서 Android 기본 폰트 패딩으로 중앙정렬이 iOS와 다르게 보임. 대부분 low.
**해당 화면:** result, intake-form, notification-permission, consent, analysis-report, home, momstagram-post, coparenting, baby-tracker(간접), timer, poop-analyzer, cry-analyzer, sos, vaccination, lullaby, nutrition, recommendations, recommendation-list, recommendation-detail, play-learning, child-card, subscription, pregnancy-journey-detail, gdm(간접), labor-monitor, birth-bag, support, voice-settings, notification-settings, privacy
→ 수정: 정밀 정렬이 중요한 Text에 `includeFontPadding: false` (+ `textAlignVertical: 'center'`) 적용. 공통 텍스트 컴포넌트/스타일로 일괄 처리 권장.

### [공통][에러처리] 빈 catch로 실패를 조용히 삼킴 — '데이터 없음'으로 위장 + 재시도 불가
네트워크/로드/저장 실패가 빈 상태나 정상처럼 보여 사용자가 실패를 인지 못함. CLAUDE.md '에러를 조용히 삼키지 말 것' 위반.
**해당 화면:** intake-form, questions, analysis-report, kakao-channel, child-info(사진), momstagram, baby-tracker(다수), ai-analysis(저장), poop-analyzer, growth-stats(타임라인), album(diary/timeline), diary(사진), monthly-characteristic, recommendation-list, recommendation-detail, profile, gdm, mom-wellness, labor-monitor, pregnancy, mom-location-setup, support, voice-settings, notification-settings
→ 수정: catch에서 에러 상태 플래그 세팅 → '로드 실패 / 다시 시도' UI를 '데이터 없음'과 구분 표시. 저장 실패는 토스트/Alert.

### [공통][엣지케이스] `selectedChild` 없을 때 무한 스피너 (loading 영구 true)
`loading` 초기값 true + early-return으로 `setLoading(false)` 미실행 → 자녀 미선택/미로드 시 영구 스피너.
**해당 화면:** intake-form, monthly-characteristic, diary, recommendation-detail (high), home(간접)
→ 수정: early-return 전에 `setLoading(false)` 호출, 자녀 없을 때 전용 빈/안내 화면 렌더.

### [공통][규칙위반] 가짜 로딩 / 가짜 데이터 — Mock 금지 위반
실제 작업과 무관하게 로딩 시간을 강제하거나 의미 없는 수치를 표시.
**해당 화면:** questions(15초 강제 대기), ai-analysis(2초 minDelay), growth-stats(시뮬레이션 진행바 85%), nutrition(인덱스 기반 가짜 추천도%), baby-tracker(분석 3초, 데드), momstagram(상시 푸터 스피너)
→ 수정: 인위적 지연/수치 제거, 실제 응답 기준으로만 로딩 제어. 근거 없는 % 노출 금지.

### [공통][로직] 미사용 데드코드 (상태/컴포넌트/스타일/모달)
화면 전환 후 남은 미호출 함수·상태·모달·스타일이 다수. 유지보수 혼선 + 번들 낭비.
**해당 화면:** login(buttonDisabled), register(죽은 catch), set-nickname(emoji), analysis-detail(스타일), home(getWeeklyQuestion/EmptyState), mom-group(anonymous 분기), baby-tracker(patternAnalysis/analyzerPicker/formulaSetting 모달 전부 도달 불가), trait-detail(탭/요약/상세 전부), monthly-characteristic, profile(handleChangePassword/PasswordModal), pregnancy(showModal 모달+handleSave), clinic, mom-location-setup, recommendation-detail(CATEGORY_META)
→ 수정: 미사용 확정 시 제거, 의도된 기능이면 진입점 복원(특히 baby-tracker 모달 3종은 기능 누락 여부 확인 필요).

---

## High (36개)

### 규칙위반 — 사주/오행 노출
- **[high][onboarding/result][규칙위반] dominantType 원천값 그대로 출력** — innateData의 오행 산출 코드(목/화/토/금/수 또는 raw)를 변환 없이 `<Text>{dominantType}</Text>`로 노출. fiveElements는 ELEMENT_LABELS로 매핑하면서 dominantType만 누락. → 기질/성향 라벨 매핑 후 출력.
- **[high][onboarding/analysis-report][규칙위반] 확인 필요** fiveElements 원본을 EditorialCover에 직접 전달 — 컴포넌트 내부에서 火/木 형태로 렌더링되면 위반. → EditorialCover 내부 노출 여부 검증, 노출 시 변환/제거.
- (medium이지만 동일 계열) momstagram-post — dominantType 배지 직접 노출.

### 엣지케이스 / 무한 스피너 / 로직
- **[high][voice][로직] Case 1(Siri 텍스트) 경로에서 음성 리스너 미등록** — initSpeechRecognition 미호출로 연속 기록 시 result 이벤트 못 받아 무한 듣기 상태. cleanup도 누락. → 재시작 분기를 '리스너 등록 여부' 기준으로, length===0이면 init 호출.
- **[high][voice][엣지케이스] phase가 'processing'에 갇힘** — 저장 실패 시 재시도 버튼이 speechAvailable 조건으로 숨겨짐. → 재시도 버튼 노출 조건을 speechModuleRef 존재로 변경, handleRetry에서 리스너 보강.
- **[high][onboarding/intake-form][무한스피너] child 없으면 loading 영구 true** → early-return에 setLoading(false) 추가.
- **[high][onboarding/intake-form][에러처리] 질문 로드 실패를 빈 catch로 삼켜 '질문 없음'으로 오인** → 에러 플래그 + 재시도 UI, '질문 없음'과 '로드 실패' 구분.
- **[high][onboarding/questions][에러처리] analyze 실패를 삼키고 무조건 결과 화면 이동** — updateChild 안 됨, stale 리포트 노출. → 실패 플래그 후 에러+재시도, 무조건 replace 금지.
- **[high][onboarding/analysis-report][엣지케이스] fetch 실패 시 무한 재요청 루프** — deps에 loading 포함 + 종료 플래그 없음. → fetchAttempted ref 도입.
- **[high][onboarding/analysis-report][에러처리] 빈 catch로 네트워크 오류 삼킴, 재시도 없음** → error state + 다시 시도 버튼.
- **[high][onboarding/consent][로직] '전체 동의'가 선택 약관(마케팅/야간광고)까지 켜야만 체크됨** — 다크패턴 소지. → 체크 표시를 필수 항목(requiredAgreed) 기준으로 분리.
- **[high][onboarding/kakao-channel][엣지케이스] openURL 직후 즉시 라우팅 — 카카오 전환과 화면 전환 경쟁** → AppState active 복귀 감지 후 진행 또는 '계속하기' 버튼.
- **[high][home][로직] 임산부 접종달력 메뉴가 slice(0,8)에 잘려 영구 미노출** — 9번째로 push되나 잘림. → 우선순위 재정렬 또는 slice 한도 동적 조정.
- **[high][mom-group][로직] 공감 토글이 항상 +1 낙관적 업데이트** — 취소 시 카운트 역방향 깜빡임, active 상태 없음. → likedIds Set 도입, +1/-1 분기 + active 스타일.
- **[high][momstagram][엣지케이스] 푸터 '더 불러오는 중' 스피너가 로딩과 무관하게 상시 노출** (가짜 로딩) → 조건을 실제 loading에 묶기.
- **[high][momstagram][로직] 무한스크롤이 공개 피드만 대상 — 병합 리스트와 불일치** — 오래된 항목이 중간 끼어드는 점프. → 페이지네이션 소스와 표시 리스트 일치(스토어 변경은 승인 필요).
- **[high][coparenting][로직] 연결된 가족 카운트(accepted만)와 목록(pending 포함) 불일치** → 목록도 accepted 필터 또는 pending 별도 섹션.
- **[high][coparenting][엣지케이스] 문자 보내기 분기가 초기화된 invitePhone에 의존** — sms 딥링크 항상 미동작. → phone을 지역 변수로 캡처 후 사용.
- **[high][baby-tracker][렌더조건] 기본 분유량 설정 모달 도달 불가** (setFormulaSettingVisible(true) 없음) → 진입점 추가 또는 제거.
- **[high][baby-tracker][데드코드] handlePatternAnalysis 전체 미호출 + 3초 가짜 로딩 포함** → 진입점 연결+지연 제거 또는 삭제.
- **[high][baby-tracker][데드코드] 아기 분석 피커 모달 도달 불가** → 진입점 연결 또는 제거.
- **[high][timer][로직] selectedChild 인덱스 stale closure로 항상 children[0]** — 완료 Alert 잘못된 이름. → useRef 미러 또는 완료 처리를 useEffect로.
- **[high][timer][로직] progress 0일 때 progressRing opacity 0** — 원형 진행 링이 실제로 동작 안 함(단색 페이드). → SVG stroke 방식 또는 최소 opacity 매핑+이름 정정.
- **[high][ai-analysis][규칙위반] 강제 2초 minDelay 가짜 로딩** → Promise.all 묶음 제거.
- **[high][poop-analyzer][로직] LIKELIHOOD_CONFIG가 서버 likelihood와 불일치 시 항상 '보통'으로 강등** — '높음(위험)' 경고색 사라짐(의료 화면). → 서버 enum 확정+정규화, '높' 우선 매칭.
- **[high][fever][로직] 회복 추세 감지가 현재 측정을 직전 기록으로 잘못 집어 작동 안 함** — '열 내리는 중' 카드 미표시. → 현재 entry 제외한 history 전달.
- **[high][sos][엣지케이스] checkSymptoms 응답 data 없을 때 result 미초기화** — 직전 긴급도 카드 잔류(응급 화면). → data 없으면 setResult(null)+안내.
- **[high][vaccination][로직] 필터 카운트(0~30일/<0)와 탭 필터(>=-30) 임계값 불일치** — 카운트-리스트 항상 어긋남. → 임계값 단일 상수로 통일.
- **[high][lullaby][엣지케이스] 녹음 중 화면 이탈 시 녹음 유실** — cleanup이 stopAndUnload만 호출, 이름 모달 미진입. → unmount 시 URI 확보/임시저장 또는 폐기 안내. 2분 자동중지 race/off-by-one도 분리.
- **[high][lullaby][엣지케이스] lastCrySound 기본값 'twinkle'이 목록 제외 트랙** — 울음 감지 자동재생이 무음 실패. → 기본값을 재생 가능 트랙으로, source 없으면 폴백.
- **[high][nutrition][로직] 한국어 조사 '를/을' 분기가 사실상 항상 '를'** — '민준를' 등 오표기. → 종성 유니코드 계산으로 을/를 선택.
- **[high][nutrition][엣지케이스] 이름 없을 때 제목 '를 위한 식단 가이드'로 깨짐** → '우리 아이' 폴백.
- **[high][nutrition][규칙위반] 인덱스 기반 가짜 추천도% (95,92,...)** — 근거 없는 mock 수치. → 실제 점수 필드 사용 또는 % 제거.
- **[high][growth-stats][로직] getPercentileLabel 4분기가 모두 동일 문자열 반환** + percentile 역방향 의미 오해(키 작은 아이 '상위 97%'). → 구간별 문구 분기, 반환 규약 정합.
- **[high][growth-stats][규칙위반] 성장 진행바가 시뮬레이션(random, 85% 정지)** → 실제 단계 매핑 또는 무한 스피너.
- **[high][diary][무한스피너] selectedChild 없을 때 listLoading 영구 true** → else 분기에서 setListLoading(false)+빈 목록.
- **[high][monthly-characteristic][무한스피너] selectedChild 없을 때 setLoading(false) 미호출** → early-return 전 로딩 정리.
- **[high][recommendation-detail][무한스피너] selectedChild/title 없으면 영구 스피너** → early-return에 setLoading(false)+안내 UI.
- **[high][subscription][로직] 무료체험 버튼이 trialDaysLeft 0/undefined 구분 못함** — 체험 종료 후에도 노출, trialUsed 단일 신호 의존. → canStartTrial 파생값으로 통일.
- **[high][pregnancy][렌더조건] development 항목만 있을 때 빈 상태 안내 사라지고 빈 화면** → 빈 판정을 flatItems 기준으로.
- **[high][pregnancy][에러처리] 타임라인 로드 실패를 삼켜 빈 화면으로 위장** → captureError + loadError 상태 + 재시도.
- **[high][gdm][엣지케이스] 산모 카드가 momWeight 없으면 키/혈액형까지 통째 사라짐** — 단일 값 게이팅. → 필드별 개별 truthy 검사.
- **[high][gdm][로직] 식단 저장 시 HH:mm을 오늘 날짜에 setHours — 미래 시각 저장 가능** → 미래면 전날 보정 또는 날짜 선택 추가.
- **[high][mom-wellness][로직] 답변 완료 검증을 하드코딩 10으로 체크** — 서버 문항 수와 불일치 시 점수 왜곡. → questions.length로 변경.
- **[high][labor-monitor][엣지케이스] 확인 필요** kick 기록 createdAt이 Firestore Timestamp 형식이면 NaN으로 '오늘 0회' 표시. → createdAt ISO 정규화/검증.
- **[high][labor-monitor][로직] 진통 분석에서 진행 중 진통 미포함** — 임계 판정 항상 한 박자 늦음. → 진행 중 진통 시작 시각 포함.
- **[high][edit-profile][로직] 별명 저장 버튼 disabled가 길이>10 검증 못 막음** — 검증 두 곳 불일치. → isValidNickname 헬퍼로 통일.
- **[high][edit-profile][로직] 부모 역할 변경이 별명 저장 버튼 눌러야만 반영** — 단독 저장 경로 없음, 별명 2자 미만이면 역할 저장 불가. → 역할 즉시 저장 또는 별도 버튼.

---

## Medium (58개)

### 규칙위반 / 가짜 데이터
- **[medium][onboarding/questions][엣지케이스] apiDoneRef가 set만 되고 미사용 — 15초 가짜 로딩** → ref 실제 사용 또는 최소 표시 1~2초로.
- **[medium][momstagram-post][규칙위반] dominantType 배지 직접 노출** + 빈 값 시 ' · '만 남음. → 라벨 변환 + 조건부 렌더.
- **[medium][analysis-report → onboarding] (위 high에 포함)**
- **[medium][poop-analyzer][규칙위반] 유니코드 이스케이프(\uXXXX) 사용** (코딩규칙 5 위반) → 평문 한글로.

### 엣지케이스 / 로직
- **[medium][voice][정렬] 진행 중 수면 세션 최신 open 선택이 문자열 시간 비교로 오작동** → 분 정수 변환 비교.
- **[medium][onboarding/result][엣지케이스] fiveElements 빈/0이면 바 너비 NaN/-Infinity** → maxVal 가드.
- **[medium][onboarding/intake-form][로직] 답변 완료 검증이 키 개수만 비교** → questions.every로.
- **[medium][onboarding/questions][엣지케이스] 미답변에 Likert 범위 밖 0 전송** → 중립값(3) 또는 완료 검증.
- **[medium][onboarding/set-nickname][로직] disabled 조건과 검증 불일치(1자 통과 후 Alert)** → disabled를 length<2로.
- **[medium][onboarding/analysis-detail][엣지케이스] firstTalk 전송 후 submitting/answer 미초기화 — 복귀 시 영구 비활성** → useFocusEffect로 리셋.
- **[medium][onboarding/consent][엣지케이스] reauth 모드인데 무조건 notification-permission 경유** — isReauth 미사용. → reauth는 홈 직행 또는 주석 정정.
- **[medium][onboarding/consent][엣지케이스] register 성공 후 setAuth 예외 시 catch가 '저장 실패' 안내 + pendingSignup.clear** — 부분 성공이 실패로 위장. → 후처리 별도 try/catch 분리.
- **[medium][onboarding/child-info][엣지케이스] 임신/태어남 탭 전환 시 폼 미초기화로 데이터 섞임** → 탭 변경 시 공유 상태 리셋.
- **[medium][onboarding/child-info][엣지케이스] Android behavior="padding" 고정으로 키보드 가림** → Platform 분기('height').
- **[medium][home][엣지케이스] 마운트 시 followup 팝업이 selectedChild 미로딩으로 거의 스킵** → loadChildren 이후 순차 실행.
- **[medium][home][엣지케이스] onRefresh가 프로액티브/공지/체험 재확인 안 함** → 필요 재확인 함께 호출.
- **[medium][mom-group][엣지케이스] honor 보기 새로고침이 loadMinePosts 호출(엉뚱한 데이터)** → viewMode==='honor'는 loadPosts.
- **[medium][mom-group][렌더조건] honor 보기에서 feed loading 공유로 무관한 스피너** → 파생 보기는 isLoading false.
- **[medium][momstagram][엣지케이스] 비공개 아님 게시글 수정 시 특정 imageUri 업로드 분기 누락** — 이미지 변경 조용히 무시. → else 분기 추가/경고.
- **[medium][momstagram][에러처리] 댓글 등록 fire-and-forget — 실패 무피드백** → await+try/catch.
- **[medium][momstagram][에러처리] 포커스 시 피드 로딩 실패 에러 피드백 없음** — 빈 상태로 위장. → 로딩/에러/빈 3분기.
- **[medium][momstagram-post][엣지케이스] 나만보기 imageUri를 업로드 없이 로컬 file://로 저장** — 재시작 시 깨짐. → 업로드 또는 영구 저장소 복사.
- **[medium][coparenting][엣지케이스] 초대 모달 닫은 뒤 role/perms/phone 미초기화** → 닫기 공통 함수로 일관 리셋.
- **[medium][coparenting][에러처리] members 조회 실패 삼킴 + isOwner 기본 true 유지** — 권한 없는 사용자에 소유자 UI 노출. → catch에서 isOwner false 또는 3-상태.
- **[medium][coparenting][로직] invitee 식별이 inviteeUserId===authUserId에만 의존** — 미검출 시 권한 전부 '제한' 오표시. → null 시 로딩/오류 또는 서버 명시 필드.
- **[medium][baby-tracker][로직] TimePicker 분이 5분 배수 아니면 표시·저장 불일치, 시 탭 시 분 조용히 변경** → value 5분 정규화 또는 활성 판정 정합.
- **[medium][baby-tracker][권한] 직접 입력(custom) 저장에 ensureCanEdit() 가드 누락** — 열람전용 멤버 로컬 저장. → handleSaveCustom에 가드 추가.
- **[medium][timer][엣지케이스] 백그라운드 중 JS 타이머 어긋남** — Quality Time 핵심 시나리오. → 종료 시각 저장+AppState 보정.
- **[medium][chatbot][렌더조건] 고정 첫 인사가 과거 대화 로드 후에도 항상 노출 — 인사 중복** → isEmpty/첫 대화 한정.
- **[medium][chatbot][로직] parentRole 호칭 매핑이 저장값과 어긋나면 항상 '맘' 폴백** — 아빠도 'OO맘'. → 저장 도메인 확인 후 매핑.
- **[medium][ai-analysis][엣지케이스] 탭 전환 시 patternError 미초기화 — 재진입 시 과거 에러 잔존** → 탭 변경 시 setPatternError('').
- **[medium][cry-analyzer][엣지케이스] 파일 재선택 시 fileMime 미리셋(stale)** → handleReset에서 fileMime 초기화.
- **[medium][cry-analyzer][로직] likelihood가 정의 외 값이면 색상 '보통'으로 강등** — 색/텍스트 불일치. → 정규화/부분일치.
- **[medium][fever][로직] 해열제 재복용 간격 240분 하드코딩** — 이부프로펜(6h)과 모순, 조기 재복용 유도(안전). → MED_INTERVAL_HR 기준 계산.
- **[medium][sos][로직] 임신부 모드에서도 항상 '대학병원 응급실' 검색 — 산부인과 분기 없음** (확인 필요: 통일 의도일 수 있음) → 분기 또는 주석 보강.
- **[medium][vaccination][엣지케이스] 등록 3개월 컷오프가 완료/전체 탭에서도 과거 접종 영구 숨김** → 액션 탭에만 적용.
- **[medium][vaccination][엣지케이스] 접종 날짜 미래/비현실 날짜 검증 없음** (13월 40일 롤오버) → 범위 재검증+미래 거부.
- **[medium][lullaby][로직] prenatal/영유아 lastCrySound 공유로 모드 경계 흐림** → cry 대상 BUILT_IN 제한.
- **[medium][lullaby][엣지케이스] 타이머 만료로 정지해도 cryDetect 켜진 채 남아 재생 재개** → 만료 시 cryDetect 함께 끔.
- **[medium][lullaby][에러처리] 마이크 권한 거부 시 스위치 깜빡임** → 권한 확인을 setCryDetect 이전으로.
- **[medium][growth-stats][로직] 과거 기록 백분위를 현재 개월 표준으로 계산** — 헤더 문구와 불일치. → 측정 시점 개월 기준.
- **[medium][growth-stats][엣지케이스] 단일 데이터 포인트 복제로 가짜 변화 라인** → 1개면 단일 값 표시 분기.
- **[medium][growth-stats][엣지케이스] 저장 후 recordDate 미초기화 + 형식 검증 없음** — 직전 기록 덮어쓰기 위험. → 오늘로 리셋+검증.
- **[medium][growth-stats][로직] 성별 'U'를 무조건 남아 표준+'남아' 라벨** → 미상 중립 처리.
- **[medium][album][에러처리] BabyAlbum generateDiary 일기 없을 때 무피드백** — PregnancyTimeline과 동작 불일치. → else Alert 추가.
- **[medium][album][로직] viewMonth가 selectedChild 변경에 미동기화** — 이전 아이 월령 고정. → selectedChild.id 변경 시 useEffect 재설정.
- **[medium][diary][엣지케이스] 자녀 전환 시 aiDiary 미초기화** — 다른 아이 일기 표시. → 자녀 변경 시 setAiDiary(null).
- **[medium][diary][엣지케이스] 자녀 전환 시 listLoading 미재설정** — 이전 자녀 기록 깜빡 노출. → fetch 전 setListLoading(true)+초기화.
- **[medium][trait-detail][로직] 화면 전환 후 대량 데드코드(탭/요약/상세 전부 미사용)** — 주석은 '12섹션 표시'라 동작 불일치. → 제거 또는 의도 재확인.
- **[medium][monthly-characteristic][에러처리] 빈 catch로 에러를 '데이터 없음'으로 위장** → console.warn + error 플래그, 미수록/실패 구분.
- **[medium][recommendation-list][엣지케이스] !category 분기가 빈 카테고리에서 '준비 중'으로 위장** → 카테고리 누락 별도 안내.
- **[medium][recommendation-list][에러처리] fetch 실패 빈 catch로 '준비 중' 위장** → error state+재시도.
- **[medium][recommendation-detail][엣지케이스] API actions/reasons 타입 단언으로 통과 — 배열 아니면 forEach 깨짐** → Array.isArray 가드.
- **[medium][recommendation-detail][에러처리] catch가 에러 객체 버려 로깅 불가** → catch(e)+console.error.
- **[medium][recommendation-detail][렌더조건] fallback 콘텐츠에도 CTA/의료출처 항상 표시** → 모두 비면 전용 빈 상태.
- **[medium][play-learning][엣지케이스] selectedChild 변경 시 expandedIdx 미초기화 — 다른 활동에 매핑** → id 변경 시 리셋, key를 안정 식별자로.
- **[medium][play-learning][스타일] 원형 배지/숫자/이모지/쉐브론 includeFontPadding 미설정** (공통).
- **[medium][child-card][엣지케이스] 성/이름 분리가 charAt(0)/slice(1)로 한국 이름 구조 불일치** (두 글자 성/외자) → 데이터 필드 사용 또는 placeholder.
- **[medium][child-card][로직] 성별 fallback 'M' 고정 — 미상을 남자로** → 'F'/'M'만 매핑, 그 외 'X'.
- **[medium][child-edit][로직] 빈 문자열로 dueDate/pregnancyNotes unset 불가** — momBloodType과 처리 불일치. → `|| null`로 통일.
- **[medium][edit-profile][엣지케이스] loadProfile 실패 시 profile=null이라 isSocialUser true 오판** — 비밀번호 분기 오류. → null 시 비밀번호 섹션 숨김/재시도.
- **[medium][edit-profile][엣지케이스] 별명 저장 후 setUser(userId??'') + 실패 시 '서버 변경 실패' 오인** → userId 없으면 차단, PUT 성공과 setUser 실패 분리.
- **[medium][edit-profile][렌더조건] 비밀번호 변경 성공 후 분기별 초기화 필드 불일치** → 공통 초기화 함수.
- **[medium][profile][데드코드] handleChangePassword/PasswordModal 미연결** — 비밀번호 변경 진입점 부재. → 메뉴 연결 또는 제거.
- **[medium][subscription][엣지케이스] PortOne type 불일치 시 재시도 동선 없음** → 결제 상태 재조회/안내.
- **[medium][subscription][에러처리] loadData catch가 plans/status 실패 미구분 — 무조건 FREE 강등** — PAID가 FREE로, 이중결제 유도. → allSettled로 분리, status 보존.
- **[medium][subscription][에러처리] IAP/복원 실패 catch 부재 — 무피드백** → catch 추가.
- **[medium][pregnancy][로직] 데드코드: 통합 기록 모달(showModal) 절대 안 열림** + 관련 상태/핸들러. → 제거 또는 진입점 연결.
- **[medium][pregnancy][엣지케이스] 죽은 handleSave가 file:// 로컬 URI 저장(영구성 정책 위반)** — 모달 부활 시 즉시 버그. → 제거 또는 업로드로 수정.
- **[medium][pregnancy][렌더조건] 주차 미설정(currentWeek=0) 시 마일스톤 빈 영역+부정확 질문** → currentWeek<=0 분기.
- **[medium][gdm][로직] 혈당 추이가 최근 14회만 쓰는데 제목/라벨 표기 불일치** → '최근 14회' 명시+라벨 균등.
- **[medium][gdm][엣지케이스] loadData 빈 응답 시 이전 records/stats 미초기화(stale)** → 항상 `?? []`로 갱신.
- **[medium][gdm][에러처리] loadData catch 무음 — 로드 실패/무기록 구분 안 됨** → 에러 뷰/재시도.
- **[medium][gdm][엣지케이스] AI 분석 자동채움이 사진 교체 후 옛 값 고착** → 사진 교체 시 자동필드 초기화.
- **[medium][mom-wellness][엣지케이스] 기록/추이 로드 실패 삼켜 '기록 없음' 위장** → 에러 상태 구분.
- **[medium][labor-monitor][로직] 진통 분석이 지속시간 미사용(간격만)** — 주석/설계 불일치, 짧은 가진통 오판. → 지속시간 반영 또는 주석 정정.
- **[medium][labor-monitor][엣지케이스] 태동 초기화가 kickRunning 안 꺼 즉시 재증가** → 의도 명확화.
- **[medium][labor-monitor][엣지케이스] 전체 초기화 시 diagStep/diagAnswers 미리셋** — ruptured 남아 골든타임 고정. → 함께 초기화.
- **[medium][labor-monitor][렌더조건] 진단 말풍선이 3회 도달 즉시 한꺼번에 노출** — 정보 과부하. → 순차 노출.
- **[medium][birth-bag][로직] '미지정' 필터가 'na'를 '지정됨'으로 취급(AND 조건)** → OR 의미로 수정, na 정책 정의.
- **[medium][birth-bag][로직] 섹션 개수와 진행률 분모의 na 처리 불일치** → 통일.
- **[medium][clinic][엣지케이스] 빈 검색결과 화면에서 emptyContainer flex:1로 레이아웃 어색** (기능 영향 없음) → minHeight/ScrollView 조정.
- **[medium][support][엣지케이스] mailto openURL 성공=전송 성공 오인, 폼 조기 초기화로 입력 유실** → 입력 유지, 확인 후에만 초기화.
- **[medium][support][로직] 앱 버전 '1.4.2' 하드코딩(현재 v2.9.0)** — 잘못된 버전 신고. → Constants에서 런타임 읽기.
- **[medium][mom-location-setup][엣지케이스] onSave 성공 후 saved.hasLocation 미갱신** → setSaved 동기화.
- **[medium][mom-location-setup][에러처리] reload 실패를 '빈 상태'로 삼킴 — 덮어쓰기 위험** → 404/오류 구분, 재시도.
- **[medium][voice-settings][로직] 수면 기본시간 onChange가 updateField 2회 호출로 입력 무시 가능** → 단일 setDefaults로.
- **[medium][voice-settings][엣지케이스] 기본값 0/빈값 무효 검증 없이 저장** → 최소값 검증.
- **[medium][notification-settings][엣지케이스] selectedChild 없을 때 토글 저장 안 됨(사일런트)** → 토글 비활성/안내.
- **[medium][notification-settings][에러처리] persist 저장/스케줄 실패 미처리 — 낙관적 UI 깨진 채 잔류** → try/catch+롤백.

---

## Low (65개)

### 데드코드 / 미사용
- **[low][login] buttonDisabled 스타일 미사용** → 통일 또는 제거.
- **[low][register] 도달 불가능한 catch(동기 흐름)** — 잘못된 '이메일 중복' 안내. → try/catch/async 제거.
- **[low][set-nickname] emoji 스타일 미사용** → 제거.
- **[low][analysis-detail] header/summaryCard 등 미사용 스타일** → 제거.
- **[low][home] getWeeklyQuestion/EmptyState/다수 스타일 미사용** → 제거.
- **[low][mom-group] anonymousPosts 파생값+익명 분기 전부 데드** → 정리.
- **[low][child-info] typeEmoji 미사용 + 핑크 버튼 색 하드코딩** → 제거/토큰화.
- **[low][monthly-characteristic] header/emptyEmoji 등 미사용 스타일** → 제거.
- **[low][clinic] actionIcon/emptyEmoji 미사용** → 제거.
- **[low][mom-location-setup] topBar 등 미사용 스타일** → 제거.
- **[low][recommendation-detail] CATEGORY_META default만 존재(분기 무의미)** → 채우거나 단순화.
- **[low][trait-detail] analysisDate 계산값 미렌더 + 변수명 오류** → 렌더 또는 제거.

### 로직 / 엣지케이스
- **[low][voice] 메인 useEffect deps 혼란(동작상 무해)** → 수정 불필요.
- **[low][onboarding/questions] options trait가 TRAIT_META에 없으면 meta 접근 깨짐** → fallback 가드.
- **[low][set-nickname] setUser(userId??'') 빈값 폴백** → 없으면 에러 처리.
- **[low][analysis-detail] useEffect deps에 loading 포함 불필요** → 제거.
- **[low][notification-permission] handleAllow finally에서 submitting 먼저 해제 — 더블탭 여지** → 순서 정리.
- **[low][consent] handleSubmit catch가 모든 실패 단일 메시지** → HTTP 상태별 분기.
- **[low][kakao-channel] submitting이 화면 이탈 직전만 해제 — 피드백 거의 없음** + openURL 실패 무피드백 → 실패 안내/라우팅 시점 이동.
- **[low][kakao-channel] 폴백 주석과 URL 배열(1개) 불일치** → 정리.
- **[low][home] 큰 이모지/SOS 글리프 정렬(공통)**.
- **[low][mom-group] feed 외 전환 시 검색 ✕ 아이콘 고정** → viewMode 변경 시 닫기.
- **[low][mom-group] radius 검색 시 qField 미전달** → 파라미터 추가 또는 칩 숨김.
- **[low][momstagram-post] createPost 응답 likes/comments 무시(0/[] 덮어씀)** → 응답값 사용.
- **[low][momstagram-post] apiPost any 흐름 + data.data??data.post??data 폴백** (코딩규칙 1 위반) → 타입+가드.
- **[low][coparenting] accept 실패 시 acceptCode 미초기화** → 정책 정의.
- **[low][baby-tracker] 미래 시각 기록 시 경과시간 0 클램프** → '계산 불가' 처리 또는 미래 입력 검증.
- **[low][chatbot] 고정 인사 createdAt을 매 렌더 new Date()** → useMemo 고정.
- **[low][chatbot] 오류 메시지 버블에 isLatest=true** — 부적절 부가 UI. → 에러 제외.
- **[low][ai-analysis] 분석 기록 저장 실패 무시로 모달 미표시** → 생성 data로 직접 모달.
- **[low][poop-analyzer] 차단 Alert 동안 콘텐츠 노출** → 차단 시 null 렌더.
- **[low][cry-analyzer] bulletNum 고정 width:16에 두 자리 잘림** → minWidth.
- **[low][cry-analyzer] possibilities 빈 항목 빈 행 렌더 + 인덱스 key** → filter+안정 key.
- **[low][fever] danger/emergency에서 119 버튼 2개 중복** → 하나만 노출.
- **[low][fever] recovering 분기 우선순위 잠재 위험** → 보수적 가드.
- **[low][sos] temperature NaN 검증 없음** → Number.isFinite.
- **[low][sos] dotRow absolute로 경고 카드와 겹칠 수 있음** → flex 배치/패딩.
- **[low][sos] scrollEventThrottle 32로 인디케이터 어긋남** → onMomentumScrollEnd 보정.
- **[low][vaccination] 완료 카드 병원명/질병 표시 들쭉날쭉** → 일관 표기.
- **[low][lullaby] 녹음 자동 이름 'N'이 삭제 후 중복** → 단조 증가 카운터.
- **[low][lullaby] 재생 중 녹음 삭제 시 currentSound find 중복** → 변수화.
- **[low][recommendations] 임신주차 누락 시 '0주차·초기' 모순** → week>0 가드. *(medium 분류였으나 표시 정리)*
- **[low][recommendations] 조사 '을(를)' 하드코딩** → 종성 계산.
- **[low][recommendation-list] useEffect deps에 나이/주차/기질 미반영** → deps 추가.
- **[low][recommendation-list] chevron 유니코드 이스케이프(규칙5 위반)+정렬** → 아이콘 컴포넌트.
- **[low][recommendation-detail] bulletDot이 공백 2칸이라 불릿 안 보임** → 실제 글리프/View.
- **[low][recommendation-detail] '더 물어보기'가 title/category 미전달** + `as never` → params 전달, 정식 타입.
- **[low][play-learning] 쉐브론 토글이 reason 항상 노출(어포던스 불일치)** → reason도 expanded 포함.
- **[low][play-learning] dominantType 영문 오행 키면 'fire/wood' 노출 가능(확인 필요)** → temperamentKey→한글 매핑.
- **[low][play-learning] playActivities.ts 다수 한글 오타가 카드 제목/본문 노출('풍선 놓이' 등)** → 오타 교정.
- **[low][child-card] D+day 라벨/값 위치 뒤바뀜** → 다른 셀과 정렬.
- **[low][child-card] months null 시 '개월'만 표시** → 조건 분기.
- **[low][child-card] handleShare catch가 사용자 취소까지 '공유 불가'** → 취소/실패 구분.
- **[low][child-edit] 비임산부 gender 'U'면 칩 미선택** → 정규화.
- **[low][child-edit] 재스케줄 dueDate(서버)/isHighRisk(로컬) 출처 불일치** → 서버 기준 통일.
- **[low][edit-profile] useEffect deps 비어 lint 경고** → useCallback.
- **[low][profile] 아이/계정 삭제 실패 빈 catch** → catch(e)+로깅, 단계 구분.
- **[low][profile] logout 후 await 대기 중 상태 불일치 구간** → router.replace 먼저 또는 fire-and-forget.
- **[low][pregnancy] 마일스톤 피드 아이콘 잎사귀 폴백으로 구분 안 됨** → ALL_MILESTONES lookup.
- **[low][pregnancy] 앨범 폼 기본 날짜 미재설정(이전 기간 잔존)** → 생성 후 초기화.
- **[low][pregnancy-journey-detail] closeGuide의 markGuideSeen 미처리** → await+catch.
- **[low][pregnancy-journey-detail] hero 배지/헤더 주차 표기 불일치(wk12)** → 단일 소스.
- **[low][gdm] 활성 탭 fontWeight 700→600 역전** → 활성 700+.
- **[low][gdm] FAB와 인라인 버튼 중복 + AdSlot에 가림** → 정리.
- **[low][gdm] 먹은 시간 25:99 비정상 시각 미검증** → 범위 검증.
- **[low][mom-wellness] 기분 저장 실패 무피드백(거짓 ✓ 저장됨)** → 성공 후에만 갱신.
- **[low][mom-wellness] 기분 추이가 1개일 때 숨겨짐('지난 7일' 라벨 혼동)** → 임계/라벨 정합.
- **[low][clinic] 키워드 변경이 useCallback deps 없어 반경 변경 시 무시** → keyword 반영.
- **[low][clinic] 상세 링크 열기 실패 빈 catch** → Alert.
- **[low][clinic] 결과 거리순 정렬 보장 없음(확인 필요)** → distance 정렬.
- **[low][support] copyEmail catch 빈 처리** → 실패 안내.
- **[low][mom-location-setup] 위치 삭제 중 로딩/disabled 없음 — 중복 호출** → removing 플래그.
- **[low][mom-location-setup] reverseGeocode가 사용자 입력 label 덮어씀** → 비었을 때만.
- **[low][voice-settings] copied 상태가 다중 URL 단계 전역 공유** → 대상별 식별.
- **[low][voice-settings] 아이 목록 빈 상태 안내 없음** → 빈 상태 UI.
- **[low][notification-settings] loadNotificationPrefs 실패 시 기본(전부 true)로 진행 — OFF 무시 위험** → 에러 상태 노출.
- **[low][notification-settings] hhmmToDate 입력 undefined 가능성** → fallback.
- **[low][privacy] 시행일 3곳 불일치(5/25 vs 4/5)** → 단일 상수.

---

## 확인 필요 (저신뢰)

- **[analysis-report]** fiveElements가 EditorialCover에서 실제로 火/木 형태로 렌더되는지 — 컴포넌트 내부 검증 후 high/무해 판단.
- **[labor-monitor]** kick createdAt이 Firestore Timestamp 직렬화 형식인지 — 서버 응답 포맷 확인 후 NaN 여부 결정.
- **[sos]** 임신부 '대학병원 응급실' 통일이 의도인지 분기 누락인지 — 제품 정책 확인.
- **[play-learning]** 저장된 dominantType에 영문 오행 키가 실제 유입되는지 — 데이터 확인 후 노출 위험 판단.
- **[clinic]** 병원 목록 API가 거리순 정렬을 보장하는지 — API 계약 확인.

---

**우선 처리 권고:** ① 사주/오행 노출 3건(출시 차단급) → ② 무한 스피너 4건 → ③ 가짜 로딩/데이터 5건 → ④ 안전 직결(fever 재복용 간격, poop/cry 위험도 색상 강등, sos 결과 잔류) → ⑤ 나머지 high. 공통 패턴(includeFontPadding, 빈 catch)은 일괄 작업으로 묶어 처리 권장.