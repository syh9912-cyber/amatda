# 안전 복귀 지점 — 2026-04-30

> 오늘(4/30) 작업량이 많아 후속 변경에서 회귀 발생 시 이 지점으로 되돌리기 위한 기록.

---

## 현재 상태 (이 지점이 "정상 작동" 기준)

### Git
- **태그**: `safe-2026-04-30-pre-pregnancy-overhaul`
- **HEAD 커밋**: `3171acd fix: 임신부 모드 P0 버그 + 태동/진통 UX 개선`
- **브랜치**: `release/v2.9.0`

### 배포 상태
| 컴포넌트 | 버전/식별자 |
|---------|-----------|
| Firebase Functions `api` | 커밋 `3171acd` 빌드 (사주 인터프리터 + 결제 Phase 1 + 임신부 P0 fix) |
| Firebase Functions `coachingApi` | 동일 |
| Firebase Hosting | `index.html / privacy.html / terms.html / refund.html / checkout.html / google verification` |
| EAS OTA preview 채널 | Update Group `576190db-cb83-4005-b792-10a9b7f751b9` (Runtime 2.8.1) |
| Firestore 인덱스/룰 | 변경 없음 |

### 정상 작동 검증된 기능
- 베이비 트래커 (수유/수면/분유텀/메모)
- 기질 분석 (Gemini 해석 + 사주 용어 차단)
- 임신부: 엄마 상태 기록 / 태동 체크 (저장+삭제) / 진통 체크 (분석 카드)
- 안심 가이드 모달 / 면책 고지
- SOS (영유아/임신부 분기)

### 미작동 (예정대로 — 키 발급 후 활성화)
- 결제 (PortOne / IAP) — 환경변수 미등록 상태, 화면만 준비됨
- expo-iap 네이티브 모듈 — 다음 EAS 빌드에서 활성화

---

## 🔁 복귀 방법

### 시나리오 A — Git 코드만 되돌리기 (배포는 그대로)
```bash
git checkout release/v2.9.0
git reset --hard safe-2026-04-30-pre-pregnancy-overhaul
```

### 시나리오 B — 백엔드까지 되돌리기
```bash
git reset --hard safe-2026-04-30-pre-pregnancy-overhaul
cd backend && npm run build && cd ..
firebase deploy --only functions:api,functions:coachingApi
```

### 시나리오 C — 프론트엔드 OTA까지 되돌리기 (가장 흔한 케이스)
```bash
git reset --hard safe-2026-04-30-pre-pregnancy-overhaul
cd frontend
npx eas update --branch preview --environment preview \
  --message "되돌리기: safe-2026-04-30 지점으로 복원" \
  --non-interactive
```
앱 재시작 시 사용자에게 자동 적용됨.

### 시나리오 D — 빠른 OTA 롤백 (EAS 기본 기능)
git 손대지 않고 EAS Update 자체 롤백 사용:
```bash
cd frontend
# preview 브랜치 최신 1단계 전 update group으로 되돌림
npx eas update:rollback --branch preview --non-interactive
```
또는 명시적으로:
```bash
npx eas channel:edit preview --update 576190db-cb83-4005-b792-10a9b7f751b9
```

### 시나리오 E — 전체 통째로 되돌리기 (백엔드 + OTA)
```bash
git reset --hard safe-2026-04-30-pre-pregnancy-overhaul
cd backend && npm run build && cd ..
firebase deploy --only functions:api,functions:coachingApi
cd frontend && npx eas update --branch preview --environment preview \
  --message "되돌리기: safe-2026-04-30" --non-interactive
```

---

## 주의

- **Firestore 데이터는 git으로 못 되돌림.** 새로 만든 컬렉션(`payments`, `billingKeys`, `kickSessions` 등)에 데이터가 쌓였다면 데이터는 남아있음 (해는 없음, 무시).
- **새로 등록된 아이의 사주 분석 결과**는 4/29 이후 등록되었다면 새 인터프리터 결과로 저장되어 있음 (되돌려도 그 데이터는 그대로). 재계산하고 싶으면 사용자가 프로필에서 생년월일/시간을 1분 수정 후 저장.
- **EAS 빌드 자체는 변경 없음** — 네이티브 코드 변경(expo-iap, react-native-webview)은 다음 build부터 적용 예정. 현재 OTA만 변경됨.

---

## 다음 작업 시작 전 권장

1. 새 작업 시작 전: `git status`로 깨끗한 상태 확인
2. 단계별로 작은 커밋 (회귀 추적 쉬움)
3. 큰 변경 후: 반드시 폰에서 직접 확인 → 문제없으면 다음 단계
4. 회귀 발견 시 위 시나리오 중 하나로 즉시 복귀

---

생성일: 2026-04-30
